apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: petclinic
  name: petclinic-route
spec:
  port:
    targetPort: 8080-tcp
  to:
    kind: Service
    name: petclinic-service
    weight: 100
  wildcardPolicy: None
